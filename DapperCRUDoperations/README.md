# Dapper CRUD Operations

## 📌 Overview
This project demonstrates how to implement **CRUD (Create, Read, Update, Delete)** operations in C# using **Dapper**, a lightweight micro-ORM for working with SQL databases.

## 🎯 Purpose
- Practice connecting a .NET application to a database using a connection string
- Execute SQL queries/commands from C# with **Dapper**
- Map database rows to C# objects (and vice-versa)
- Understand a clean flow for CRUD without using a full ORM (e.g., Entity Framework)

## 🧠 Concepts Practiced
- Dapper basics (`Query`, `QueryFirstOrDefault`, `Execute`)
- Parameterized SQL commands (safe query parameters)
- Mapping results to POCO classes (models)
- Insert / Select / Update / Delete patterns
- Connection management (`IDbConnection`, `using` / disposal)
- Basic separation of concerns (data access vs business logic)

## 🛠 Tech Stack
- **C#**
- **.NET**
- **Dapper**
- **SQL Database** (e.g., SQL Server)
